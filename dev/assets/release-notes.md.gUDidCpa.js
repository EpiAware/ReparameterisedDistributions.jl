import{_ as a,o as t,c as o,an as r}from"./chunks/framework.BdTLjat_.js";const m=JSON.parse('{"title":"Release notes","description":"","frontmatter":{},"headers":[],"relativePath":"release-notes.md","filePath":"release-notes.md","lastUpdated":null}'),n={name:"release-notes.md"};function i(d,e,s,c,l,h){return t(),o("div",null,[...e[0]||(e[0]=[r('<h1 id="Release-notes" tabindex="-1">Release notes <a class="header-anchor" href="#Release-notes" aria-label="Permalink to &quot;Release notes {#Release-notes}&quot;">​</a></h1><p>ReparameterisedDistributions.jl follows a hybrid changelog approach:</p><ul><li><p><strong>GitHub Releases</strong>: All releases (automatically generated by TagBot)</p></li><li><p><strong>NEWS.md</strong>: Major releases requiring detailed context (shown below)</p></li></ul><p>See <a href="https://github.com/EpiAware/ReparameterisedDistributions.jl/releases" target="_blank" rel="noreferrer">GitHub Releases</a> for minor/patch release information.</p><h2 id="Unreleased" tabindex="-1">Unreleased <a class="header-anchor" href="#Unreleased" aria-label="Permalink to &quot;Unreleased {#Unreleased}&quot;">​</a></h2><h3 id="The-moment-parameterised-wrapper" tabindex="-1">The moment-parameterised wrapper <a class="header-anchor" href="#The-moment-parameterised-wrapper" aria-label="Permalink to &quot;The moment-parameterised wrapper {#The-moment-parameterised-wrapper}&quot;">​</a></h3><p><code>reparameterise(dist_or_type; moments...)</code> wraps a Distributions.jl family so that the quantities a modeller reasons about are its parameters. The result is an ordinary <code>Distribution</code>: it evaluates and samples exactly as the native one does and goes on the right of a <code>~</code>, so a model puts priors directly on a mean and a standard deviation and samples in those coordinates. <code>params</code> reports the moments, not the native parameters that only imply them.</p><p>The conversion to the native family is exact algebra rather than a numerical solve, so it is differentiable; gradients with respect to the moments are checked against ForwardDiff, ReverseDiff, Enzyme (forward and reverse) and Mooncake (forward and reverse).</p><p>Supported: <code>LogNormal</code> by <code>(mean, sd)</code> or <code>(mean, var)</code>; <code>Gamma</code> by <code>(mean, sd)</code>, <code>(mean, var)</code> or <code>(mean, shape)</code>; and <code>NegativeBinomial</code> by <code>(mean, overdispersion)</code>, where the overdispersion is the excess variance relative to a Poisson (<code>var = mean + overdispersion * mean^2</code>).</p><p>A wrapper takes its variate form and value support from the family it wraps, so a <code>NegativeBinomial</code> wrapper stays discrete.</p><p>The moments are validated in their own coordinates, not merely through the native distribution they imply. The LogNormal and Gamma conversions square the standard deviation, so a negative one would otherwise map onto a perfectly valid native distribution and the wrapper would report a parameter it does not behave as.</p><h3 id="native-and-to_native-are-now-exported" tabindex="-1"><code>native</code> and <code>to_native</code> are now exported <a class="header-anchor" href="#native-and-to_native-are-now-exported" aria-label="Permalink to &quot;`native` and `to_native` are now exported {#native-and-to_native-are-now-exported}&quot;">​</a></h3><p>The wrapper-level accessor and the per-family extension point were both previously underscore-prefixed internals (<code>_native</code>, <code>_to_native</code>), against the org convention that a name a caller is expected to type directly is exported rather than merely public. <code>native(d)</code> reaches the distribution a wrapper converts to — and, through it, the native parameters (<code>params(native(d))</code>) when the moments alone are not what is needed. <code>to_native(D, Val(names), vals)</code> is unchanged in behaviour; only its name and visibility change, so registering a new family from outside this package no longer needs the <code>ReparameterisedDistributions.</code> qualification.</p><h3 id="Construction-is-now-fully-type-inferred" tabindex="-1">Construction is now fully type-inferred <a class="header-anchor" href="#Construction-is-now-fully-type-inferred" aria-label="Permalink to &quot;Construction is now fully type-inferred {#Construction-is-now-fully-type-inferred}&quot;">​</a></h3><p><code>reparameterise</code> returned a <code>Reparameterised</code> with uninferred <code>names</code>, <code>N</code> and <code>T</code> type parameters for any call the compiler did not fully constant-fold — <code>@inferred</code> on it failed. The cause: the internal <code>_build</code> took the parameter names as a plain <code>Tuple{Vararg{Symbol}}</code> and constructed a <code>Val</code> from it internally, and <code>Val(runtime_value)</code> cannot be inferred concretely from a value the caller has not already carried as a type parameter. <code>_build</code> now takes <code>Val{names}</code> directly, so the whole <code>Reparameterised{...}</code> type is inferred end to end — the case that matters most is exactly the one constant folding cannot be relied on inside: an AD tape.</p><h3 id="Package-identity" tabindex="-1">Package identity <a class="header-anchor" href="#Package-identity" aria-label="Permalink to &quot;Package identity {#Package-identity}&quot;">​</a></h3><p>The package is renamed from <code>AltDistributions</code> to <code>ReparameterisedDistributions</code> to match the repository, and takes a new UUID (<code>7cd6e41d-e337-45a7-b8fc-acb99a44bf42</code>). Neither the old name nor the old UUID could be carried forward: the 2024 scaffold&#39;s UUID is now held in the General registry by <code>CensoredDistributions</code>, which was derived from that scaffold, and the name <code>AltDistributions</code> is held there by an unrelated package.</p><h3 id="Polish" tabindex="-1">Polish <a class="header-anchor" href="#Polish" aria-label="Permalink to &quot;Polish {#Polish}&quot;">​</a></h3><p>A <code>Reparameterised</code> now has a richer REPL display: alongside the code-reconstructable one-liner, <code>MIME&quot;text/plain&quot;</code> also prints the native distribution the wrapper actually evaluates as. The package gains its own logo, the getting-started page has a runnable first example rather than scaffold placeholder text, and a stray reference to an unpublished internal package has been removed from the docs.</p><h3 id="Infrastructure" tabindex="-1">Infrastructure <a class="header-anchor" href="#Infrastructure" aria-label="Permalink to &quot;Infrastructure {#Infrastructure}&quot;">​</a></h3><p>The package adopts the EpiAwarePackageTools managed standard with <code>ad = true</code>: managed CI, quality checks (Aqua, ExplicitImports, JET, docstring format, formatting), the Documenter and DocumenterVitepress docs build, a benchmark suite, and the AD-gradient harness covering ForwardDiff, ReverseDiff, Enzyme (forward and reverse) and Mooncake (forward and reverse).</p>',21)])])}const u=a(n,[["render",i]]);export{m as __pageData,u as default};
