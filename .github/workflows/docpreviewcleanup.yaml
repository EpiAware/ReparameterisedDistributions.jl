# MANAGED by EpiAwarePackageTools.scaffold — do not edit by hand.
name: Doc Preview Cleanup

on:
  pull_request:
    types: [closed]

permissions:
  contents: write

jobs:
  doc-preview-cleanup:
    uses: EpiAware/.github/.github/workflows/docs-preview-cleanup.yml@c8f00e8d2e64d3f7e7afc64666041cf8595ff7ae
    secrets: inherit  # pragma: allowlist secret
