# MANAGED by EpiAwarePackageTools.scaffold — do not edit by hand.
name: Doc Preview Cleanup

on:
  pull_request:
    types: [closed]

permissions:
  contents: write

jobs:
  doc-preview-cleanup:
    uses: EpiAware/.github/.github/workflows/docs-preview-cleanup.yml@4ade02869137af2a1799c704df8a0256ef5b5de6
    secrets: inherit  # pragma: allowlist secret
