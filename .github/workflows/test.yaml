# MANAGED by EpiAwarePackageTools.scaffold — do not edit by hand.
name: Test

on:
  push:
    branches:
      - main
  pull_request:
  merge_group:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    uses: EpiAware/.github/.github/workflows/tests.yml@8c1e09003b9cf0d2eb3cbec7aa726855bb365ac5
    with:
      # No LTS (1.10). The kit's test environment pins EpiAwarePackageTools via
      # `[sources]`, which Pkg only honours on Julia 1.11+. On 1.10 it is
      # silently ignored and the registered EpiAwarePackageTools v0.1.0 resolves
      # instead, which predates the `run_package_tests` entry point that the
      # current `test/runtests.jl` template calls. Matches ModifiedDistributions.
      julia_versions: '["1", "pre"]'
    secrets: inherit  # pragma: allowlist secret
